@model Proje.Models.YemekMalzeme

@{
    ViewData["Title"] = "Details";
}
@if (User.IsInRole("Admin"))
{
    <h1>Details</h1>

    <div>
        <h4>YemekMalzeme</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.MalzemeMiktari)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.MalzemeMiktari)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Yemek)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Yemek.Id)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Malzeme)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Malzeme.MalzemeId)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
        <a asp-action="Index">Back to List</a>
    </div>
}
else
{
    <div>
        <a asp-action="Index">Yetkili Değilsiniz!!!</a>
    </div>
}
<script>
    function SetCulture(selectedValue) {
        alert(selectedValue);
        var url = window.location.href.split('?')[0];
        var culture = "?culture=" + selectedValue + "&ui-culture=" + selectedValue;
        window.location.href = url + culture;
    }
</script>
